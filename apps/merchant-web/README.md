# TingGo Merchant Web (Next.js + TypeScript)

Web quản lý quán: menu, bàn/QR, order board, nhân viên, báo cáo (PRD MER-01..08).

Init (Sprint 1):
```bash
npx create-next-app@latest . --typescript --app --tailwind --eslint --src-dir
npm i @microsoft/signalr
npx shadcn@latest init   # UI component theo Shadcn
```
